#include <stdlib.h>
#include <stdio.h>
#include <math.h>

/*
void pin() {
    char c[10];
    int pin;
    printf("pinON: \n");
    
    for (int i = 0;c != '\n'; i++) {
        scanf("%c", &c[i]);
        pin = (int) atoi(c)*powf((float) 10, (float) i);
    }
    printf("pinL: %d\n", pin);
}
*/
void pin() {
    
}

int main (void) {
    while(1) {
        pin();
    }
    return 0;
}